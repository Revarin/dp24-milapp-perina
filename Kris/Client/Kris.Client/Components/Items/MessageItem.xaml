<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:tk="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:vm="clr-namespace:Kris.Client.ViewModels.Items"
             x:Class="Kris.Client.Components.Items.MessageItem"
             x:DataType="vm:MessageItemViewModel"
             BackgroundColor="DimGrey">
    
    <StackLayout Orientation="Horizontal"
                 HorizontalOptions="FillAndExpand">
        <StackLayout Orientation="Vertical"
                     HorizontalOptions="FillAndExpand">
            <StackLayout Orientation="Horizontal"
                         HorizontalOptions="FillAndExpand">
                <Label Text="{Binding SenderName}" FontSize="Small" FontAttributes="Bold" />
                <Label Text="{Binding Sent}" FontSize="Small" />
            </StackLayout>

            <Label Text="{Binding Body}" />
        </StackLayout>
    </StackLayout>
</ContentView>
