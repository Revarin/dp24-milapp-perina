<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Kris.App.Map;assembly=Kris.App.Map"
             x:Class="Kris.App.Views.TestPage"
             Title="Menu page"
             BackgroundColor="{DynamicResource PageBackgroundColor}">
    <ContentPage.BindingContext>
        <local:Test />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Random MENU text"
               Style="{StaticResource LargeLabelStyle}"
               VerticalOptions="CenterAndExpand" 
               HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding Text}"
               Style="{StaticResource MediumLabelStyle}"
               VerticalOptions="CenterAndExpand"
               HorizontalOptions="CenterAndExpand" />
            <Button Text="Change text">
                <Button.Behaviors>
                    <local:Test EventName="Clicked" Command="{Binding SwitchTextCommand}" />
                </Button.Behaviors>
            </Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
